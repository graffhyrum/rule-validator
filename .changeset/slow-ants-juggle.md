---
"@graff/rule-validator": minor
---

Add production dependencies and build configuration: arktype for validation, commander for CLI, glob for file scanning. Add biome and changesets as dev dependencies.
